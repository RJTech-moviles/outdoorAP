<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OUTDOORAP.NewPage1"
             Title="NewPage1">
    <ScrollView>
        <VerticalStackLayout>

            <Label
                Text="correo@correo"
                SemanticProperties.HeadingLevel="Level3"
                SemanticProperties.Description="Ir a una nueva vista"
                FontSize="16"
                HorizontalOptions="Center" 
                VerticalOptions="Center"/>

            <Label
                Text="55 5555 5555"
                SemanticProperties.HeadingLevel="Level3"
                SemanticProperties.Description="Ir a una nueva vista"
                FontSize="16"
                HorizontalOptions="Center" 
                VerticalOptions="Center"/>

            <Button Text="Enviar Codigo"  
                HorizontalOptions="Center"
                FontSize="16"
                VerticalOptions="Center"/>
            
            <!-- Aqui van las lineas del codigo de recuperacion -->

            <Button Text="Enviar Codigo"  
                HorizontalOptions="Center"
                FontSize="16"
                VerticalOptions="Center"/>
            
            <!-- Validar que al recibir el codigo mande a la vista de confirmarPassword -->

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>